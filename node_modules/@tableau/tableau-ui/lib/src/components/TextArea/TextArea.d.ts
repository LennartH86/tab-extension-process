import * as React from 'react';
import { TextAreaAttrs } from '../../utils/NativeProps';
import { LabeledFieldProps } from '../LabeledField';
export interface TextAreaProps extends Omit<LabeledFieldProps, 'labelRef'> {
    /**
     * When true, the `<textarea>` will automatically grow or shrink its height to match its content.
     * Using this prop will override any `height` provided in the `style` prop.
     */
    autoHeight?: boolean;
}
/**
 * All undocumented props will be passed to the native `<textarea>` element.
 */
export declare const TextArea: React.ForwardRefExoticComponent<TextAreaProps & TextAreaAttrs & React.RefAttributes<HTMLTextAreaElement>>;

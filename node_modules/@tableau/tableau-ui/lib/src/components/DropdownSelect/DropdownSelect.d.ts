import * as React from 'react';
import { SelectAttrs } from '../../utils/NativeProps';
export interface DropdownSelectProps extends SelectAttrs {
    /** NOTE: applied to the root `<div>`, not the `<select>`. */
    className?: string;
    /**
     * * `"outline"` shows a border around the entire field
     * * `"line"` shows a bottom-only border
     * * `"text"` shows no border
     * * `"icon"` show no border, and with a smaller arrow icon
     * @default '"outline"'
     */
    kind?: 'outline' | 'line' | 'text' | 'icon';
    /** Label to display above the field. Must be provided to be WCAG-compliant. */
    label?: React.ReactNode;
    /**
     * @deprecated
     * This control is not compatible with the `multiple` mode of `<select>` and thus `multiple` should never be passed.
     */
    multiple?: never;
}
/**
 * Documented props will be used by this component. All other provided props will be passed to the native `<select>` element.
 */
export declare const DropdownSelect: React.ForwardRefExoticComponent<DropdownSelectProps & React.RefAttributes<HTMLSelectElement>>;

import * as React from 'react';
import { InputAttrs } from '../../utils/NativeProps';
import { LabeledFieldProps } from '../LabeledField';
export interface TextFieldProps extends Omit<LabeledFieldProps, 'labelRef'> {
    /**
     * * `"outline"` shows a border around the entire field
     * * `"line"` shows a bottom-only border
     * * `"search"` shows as 'outline', but with a search icon
     * @default '"outline"'
     */
    kind?: 'outline' | 'line' | 'search';
    /** Called when the user indicates to clear the text of the field. */
    onClear?: () => void;
}
/**
 * Documented props will be used by this component. All other provided props will be passed to the native `<input>` element.
 */
export declare const TextField: React.ForwardRefExoticComponent<TextFieldProps & InputAttrs & React.RefAttributes<HTMLInputElement>>;

import * as React from 'react';
import { DivAttrs } from '../../utils/NativeProps';
export interface PillProps extends DivAttrs {
    /**
     * * `"continuous"`: green pill
     * * `"discrete"`: blue pill
     * * `"invalid"`: red pill
     * * `"other"`: gray pill
     */
    kind: 'continuous' | 'discrete' | 'invalid' | 'other';
    /**
     * * `false`: pills have richer coloring, matching the style of Pills in Tableau Cards and Shelves
     * * `true`: pills have lighter coloring, matching the style of Pills in the Tableau Schema Pane
     * @default false
     */
    schema?: boolean;
    /**
     * When true, coloring is darkened to indicate the Pill is selected.
     * @default false
     */
    selected?: boolean;
}
/**
 * Documented props will be used by this component. All other provided props will be passed to the root `<div>` element.
 */
export declare const Pill: React.ForwardRefExoticComponent<PillProps & React.RefAttributes<HTMLDivElement>>;

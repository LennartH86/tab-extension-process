import * as React from 'react';
import { InputAttrs } from '../utils/NativeProps';
export interface SelectorProps extends InputAttrs {
    /**
     * These props will be rested to the root `<label>` element.
     * All other props are rested to the inner `<input>` element.
     * `className` and `style` are ignored.
     */
    labelProps?: React.LabelHTMLAttributes<HTMLLabelElement>;
}
export declare const FocusVisibleClassName = "focus-visible";
export declare const FocusWithinClassName = "focus-within";
export declare const Selector: React.ForwardRefExoticComponent<SelectorProps & {
    indeterminate?: boolean | undefined;
} & React.RefAttributes<HTMLInputElement>>;

import * as React from 'react';
import { ElemAttrs } from '../../utils/NativeProps';
export interface SpinnerProps {
    /** Explanatory text (e.g. "Loading...") that should be presented by assistive technology, e.g. screen readers. */
    alt: string;
    /**
     * * `"dark"` sets the spinner color to a dark grey
     * * `"light"` sets the spinner color to a light grey
     * @default '"dark"'
     */
    color?: 'dark' | 'light';
    /**
     * The dimension of the spinner. Standard sizes are 50px and 14px.
     * @default ```50```
     */
    dimension?: number;
    /**
     * Determines how the Spinner is exposed to assistive technology.
     *
     * * `"inline"` Spinner will present as an Image role with the specified `alt` text.
     * Use this when a Spinner is placed with other inline content, e.g. next to text in a button.
     *
     * * `"status"` Spinner will present as a Status role labelled by the specified `alt` text.
     * Use this when a Spinner is used for an overall pending state, e.g. the contents of a still-loading dialog.
     * @default ```'inline'```
     */
    presentation?: 'inline' | 'alert';
    /**
     * When true, applies a semi-transparent underlay.
     *
     * NOTE: When true, the dimensions of the overall component will be 1.3x the passed `dimension`, while the
     * spinner graphic itself will be `dimension` pixels square.
     * @default false
     */
    showUnderlay?: boolean;
}
export declare const UnderlayDimensionMultiplier = 1.3;
export declare const SvgSpinnerAnimationName: string;
/**
 * An inline spinner to indicate e.g. a loading state.
 */
export declare const Spinner: React.ForwardRefExoticComponent<SpinnerProps & ElemAttrs<HTMLElement> & React.RefAttributes<HTMLDivElement>>;

import * as React from 'react';
import { ElemAttrs } from '../../utils/NativeProps';
import { ButtonProps } from '../Button/Button';
import { TextFieldProps } from '../TextField/TextField';
export interface TextFieldGroupProps extends ElemAttrs {
    /**
     * The button.
     *
     * @example
     *   button={<Button kind='outline'>Create</Button>}
     *
     * @type ReactElement<ButtonProps>
     */
    button: React.ReactElement<ButtonProps>;
    /**
     * The text field.
     *
     * @example
     *   textField={<TextFieldWidget kind='outline' label='Hierarchy Name' />}
     *
     * @type ReactElement<TextFieldProps>
     */
    textField: React.ReactElement<TextFieldProps>;
}
/**
 * A component which visually groups a `TextField` with a `Button`.
 *
 * All props not defined in `TextFieldGroupProps` will be rested to the root element.
 */
export declare const TextFieldGroup: React.FC<TextFieldGroupProps>;

import * as React from 'react';
import { ElemAttrs } from '../../utils/NativeProps';
export interface StickerProps extends ElemAttrs {
    /** The content to be displayed inside the Sticker. */
    children: React.ReactNode;
    /** Set to true when rendering on a dark background. This affects border color of the Sticker when it is interactive (i.e. has a handler for `onClick`). */
    darkBackground?: boolean;
    /** Set to true to disable the Sticker when it is interactive (i.e. has a handler for `onClick`). */
    disabled?: boolean;
    /** The type of Sticker to display. This affects the styling of the Sticker. */
    stickerType: 'neutral' | 'red' | 'yellow' | 'green' | 'teal' | 'blue' | 'violet';
}
export declare type StickerType = NonNullable<StickerProps['stickerType']>;
/**
 * Sticker is a visual marker that represents a status or classification that users add to their content.
 * When `onClick` prop is provided, Sticker is rendered as interactive.
 * All undocumented props will be passed to the root element.
 */
export declare const Sticker: React.FC<StickerProps>;

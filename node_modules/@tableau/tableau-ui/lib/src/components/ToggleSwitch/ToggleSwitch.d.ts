import * as React from 'react';
import { InputAttrs } from '../../utils/NativeProps';
export interface ToggleSwitchProps extends InputAttrs {
    /** NOTE: applied to the root `<label>`, not the `<input>`. */
    className?: string;
    /** NOTE: applied to the root `<label>`, not the `<input>`. */
    style?: React.CSSProperties;
    /** @default "left" */
    textAlign?: 'left' | 'right';
}
/**
 * Documented props will be used by this component. All other provided props will be passed to the native `<input>` element.
 */
export declare const ToggleSwitch: React.ForwardRefExoticComponent<ToggleSwitchProps & React.RefAttributes<HTMLInputElement>>;

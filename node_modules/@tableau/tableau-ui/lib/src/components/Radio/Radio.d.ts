import * as React from 'react';
import { SelectorProps } from '../Selector';
export interface RadioProps extends SelectorProps {
    /** NOTE: applied to the root `<label>`, not the `<input>`. */
    className?: string;
    /** NOTE: applied to the root `<label>`, not the `<input>`. */
    style?: React.CSSProperties;
}
/**
 * All undocumented props will be passed to the native `<input>` element.
 *
 * Renders as an inline block element by default. To render as a block element, pass `display: flex` in the `style` prop.
 */
export declare const Radio: React.ForwardRefExoticComponent<RadioProps & React.RefAttributes<HTMLInputElement>>;

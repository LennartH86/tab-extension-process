import * as React from 'react';
export declare const AnimationDuration = 200;
export interface HeightAnimatorProps {
    /** Callback function to be called when the close animation has completed */
    onExitAnimationComplete?: () => void;
    /**
     * By default this widget triggers animations based on the addition/removal of its children
     * Using the height prop, will instead trigger animation based on this value changing
     * This prop is to be used when you would like to animate an element without removing it from the DOM
     */
    height?: 0 | 'auto';
    /** Sets an initial height, which either allows/prevents an initial animation */
    initialHeight?: 0 | 'auto';
}
export declare const HeightAnimator: React.FC<HeightAnimatorProps>;
